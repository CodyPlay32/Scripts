-- Script para Delta Executor - Auto-expulsión cuando un administrador entre
local admins = {
    "darkwolfplayerinuse",     
    "s3r31n_n",
    "RaaneshGamer4435K",
    "Mvspro12321",
    "DxxrkVenom",
    "RealGloxh",
    "vicekeeper",
    "SempaiH9",
    "24l05l09",
    "coolkidsuper500",
    "S1verlynne",
    "cclarify",
    "nukestar",
    "swagknife19",
    "beodnt",
    "Sama_roblox34",
    "1Daxito",
    "Juanpith0rik0",
    "FreedomBirth",
    "dytona_x",
    "Souligraphy",
    "stuckwithpoli",
    "Imjustaproguy2232",
    "OmegaAnoobis",
    "NotVpxr",
    "lichugamerpro",
    "Brockkodile",
    "Atavi",
    "ZacksFy",
    "BossSpax",
    "yHasteeD",
    "WtrMelonAdam",
    "FamedChris",
    "DBS_WorkflowBot",
    "dbs_johnny",
    "decim8or",
    "EnlocK",
    "DBSRedis",
    "BrickStormer01",
    "Blade_Executive"
}

-- Crear interfaz de usuario para Delta
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local Status = Instance.new("TextLabel")
local AdminList = Instance.new("ScrollingFrame")
local UIListLayout = Instance.new("UIListLayout")
local ToggleButton = Instance.new("TextButton")

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
MainFrame.BorderColor3 = Color3.fromRGB(27, 42, 53)
MainFrame.BorderSizePixel = 2
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -150)
MainFrame.Size = UDim2.new(0, 400, 0, 300)
MainFrame.Active = true
MainFrame.Draggable = true

Title.Parent = MainFrame
Title.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
Title.BorderSizePixel = 0
Title.Position = UDim2.new(0, 0, 0, 0)
Title.Size = UDim2.new(0, 400, 0, 30)
Title.Font = Enum.Font.SourceSansBold
Title.Text = "Auto-Kick para Administradores"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 18.000

Status.Parent = MainFrame
Status.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
Status.BorderSizePixel = 0
Status.Position = UDim2.new(0, 0, 0, 30)
Status.Size = UDim2.new(0, 400, 0, 25)
Status.Font = Enum.Font.SourceSans
Status.Text = "Estado: Activo - Monitoreando admins"
Status.TextColor3 = Color3.fromRGB(85, 255, 85)
Status.TextSize = 14.000

AdminList.Parent = MainFrame
AdminList.Active = true
AdminList.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
AdminList.BorderSizePixel = 0
AdminList.Position = UDim2.new(0, 10, 0, 60)
AdminList.Size = UDim2.new(0, 380, 0, 180)
AdminList.CanvasSize = UDim2.new(0, 0, 0, 0)
AdminList.ScrollBarThickness = 8

UIListLayout.Parent = AdminList
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 2)

ToggleButton.Parent = MainFrame
ToggleButton.BackgroundColor3 = Color3.fromRGB(85, 170, 255)
ToggleButton.BorderSizePixel = 0
ToggleButton.Position = UDim2.new(0, 125, 0, 250)
ToggleButton.Size = UDim2.new(0, 150, 0, 30)
ToggleButton.Font = Enum.Font.SourceSansBold
ToggleButton.Text = "Desactivar"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 16.000

-- Crear etiquetas para cada administrador
for i, adminName in ipairs(admins) do
    local adminLabel = Instance.new("TextLabel")
    adminLabel.Parent = AdminList
    adminLabel.BackgroundColor3 = Color3.fromRGB(55, 55, 55)
    adminLabel.BorderSizePixel = 0
    adminLabel.Size = UDim2.new(0, 370, 0, 20)
    adminLabel.Font = Enum.Font.SourceSans
    adminLabel.Text = adminName
    adminLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    adminLabel.TextSize = 14.000
    adminLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    -- Actualizar tamaño del canvas
    AdminList.CanvasSize = UDim2.new(0, 0, 0, i * 22)
end

-- Variables de control
local isActive = true
local checkInterval = 3

-- Función para verificar si es administrador
local function isAdmin(playerName)
    for _, adminName in ipairs(admins) do
        if adminName == playerName then
            return true
        end
    end
    return false
end

-- Función para auto-expulsarse (desconectarse del servidor)
local function selfKick()
    game.Players.LocalPlayer:Kick("Administrador detectado - Auto-expulsión activada")
end

-- Verificar si hay administradores en el servidor
local function checkForAdmins()
    if not isActive then return end
    
    for _, player in ipairs(game.Players:GetPlayers()) do
        if isAdmin(player.Name) then
            Status.Text = "¡Administrador detectado: " .. player.Name .. "!"
            Status.TextColor3 = Color3.fromRGB(255, 85, 85)
            
            -- Esperar un momento antes de auto-expulsarse
            wait(1)
            
            -- Auto-expulsarse
            selfKick()
            return
        end
    end
end

-- Comprobar cuando un jugador se une
game.Players.PlayerAdded:Connect(function(player)
    if not isActive then return end
    
    if isAdmin(player.Name) then
        Status.Text = "¡Administrador entró: " .. player.Name .. "!"
        Status.TextColor3 = Color3.fromRGB(255, 85, 85)
        
        -- Esperar un momento antes de auto-expulsarse
        wait(1)
        
        -- Auto-expulsarse
        selfKick()
    end
end)

-- Botón de activar/desactivar
ToggleButton.MouseButton1Click:Connect(function()
    isActive = not isActive
    if isActive then
        ToggleButton.Text = "Desactivar"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(85, 170, 255)
        Status.Text = "Estado: Activo - Monitoreando admins"
        Status.TextColor3 = Color3.fromRGB(85, 255, 85)
    else
        ToggleButton.Text = "Activar"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(255, 85, 85)
        Status.Text = "Estado: Inactivo"
        Status.TextColor3 = Color3.fromRGB(255, 85, 85)
    end
end)

-- Comprobación periódica
spawn(function()
    while wait(checkInterval) do
        checkForAdmins()
    end
end)

print("Script de auto-expulsión para Delta cargado correctamente")
